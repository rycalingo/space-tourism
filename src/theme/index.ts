import { extendTheme } from "@chakra-ui/react";


const main = {
  colors: {

  },

  fonts: {

  },

  styles: {
    global: () => ({
      body: {
        color: "#fff",
        bgColor: "#000000",
      },
    }),
  },

  components: {

	},
}

export default extendTheme(main);
